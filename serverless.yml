name: socodeweb
org: socode
component: website
stage: production

inputs:
  src:
    src: ./src
    index: index.html
    hook: npm run build_and_route
    dist: ./build
    websitePath: ./
  region: ap-shanghai
  bucketName: socode
  protocol: https
